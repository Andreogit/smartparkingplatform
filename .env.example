# Compose variable substitution (copy to `.env` in repo root).
# Backend secrets live in `backend/.env` — see `backend/.env.example`.

POSTGRES_USER=bkr
POSTGRES_PASSWORD=bkr
POSTGRES_DB=bkr

POSTGRES_PUBLISH_PORT=5432
BACKEND_PUBLISH_PORT=3000

BACKEND_ENV_FILE=./backend/.env
